#Service ID
dismiss_alert:
  name: Dismiss alert
  description: Dismiss an alert
  target:
    entity:
      integration: rtl_433
      domain: binary_sensor

dismiss_alerts:
  name: Reset Motor
  description: Dismiss all alerts
  target:
    entity:
      integration: rtl_433
      domain: binary_sensor
